sed -i 's/val weatherWarning by viewModel.weatherWarning.collectAsStateWithLifecycle()/val isLoading by viewModel.isLoading.collectAsStateWithLifecycle()\n    val weatherWarning by viewModel.weatherWarning.collectAsStateWithLifecycle()/g' app/src/main/java/com/example/MainActivity.kt
sed -i 's/if (historyItems.isNotEmpty()) {/if (isLoading) {\n            Box(modifier = Modifier.fillMaxWidth().height(120.dp), contentAlignment = Alignment.Center) {\n                CircularProgressIndicator(modifier = Modifier.size(36.dp), color = MaterialTheme.colorScheme.primary)\n            }\n        } else if (historyItems.isNotEmpty()) {/g' app/src/main/java/com/example/MainActivity.kt
